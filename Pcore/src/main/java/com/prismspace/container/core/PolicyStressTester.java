package com.prismspace.container.core;

/*
import android.util.Log;
import java.io.File;
import java.util.Random;
import java.util.concurrent.atomic.AtomicBoolean;
import java.util.concurrent.atomic.AtomicLong;

import com.prismspace.container.PrismSpaceCore;
*/

/**
 * H6: Integration Stress Tester (Disabled for Build)
 */
public class PolicyStressTester {
    /*
    private static final String TAG = "PrismStress";
    private static final int RULE_COUNT = 10;
    private static final long UPDATE_INTERVAL_MS = 2000;
    private static final long EVALUATION_INTERVAL_MS = 2000;

    private final AtomicBoolean mIsRunning = new AtomicBoolean(false);
    private final AtomicLong mGeneration = new AtomicLong(3000);
    
    private PolicyPublisher mPublisher;
    private GreenLightEvaluator mEvaluator = new GreenLightEvaluator();

    public void startIntegrationTest() {
        if (mIsRunning.getAndSet(true)) return;
    }

    public void stopTest() {
        mIsRunning.set(false);
    }
    */
}
